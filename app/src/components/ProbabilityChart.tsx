import { StyleSheet, Text, View } from 'react-native';
import { colors, marketOutcomeColors } from '../theme/colors';
import type { Outcome } from '../types/market';
export function ProbabilityChart({ outcomes }: { outcomes: Outcome[] }) { return <View style={styles.chart}>{[0,1,2,3].map((line) => <View key={line} style={[styles.grid, { top: 20 + line * 61 }]} />)}{outcomes.slice(0, 4).map((outcome, index) => { const outcomeColor = outcome.color ?? marketOutcomeColors[index] ?? colors.accent; return <View key={outcome.label} style={[styles.plot, { top: 39 + index * 47, backgroundColor: outcomeColor }]}><View style={[styles.point, { backgroundColor: outcomeColor }]} /></View>; })}<Text style={styles.leftDate}>08/25 21:30</Text><Text style={styles.rightDate}>08/26 21:30</Text></View>; }
const styles = StyleSheet.create({ chart: { height: 205, marginTop: 17, position: 'relative', transform: [{ scaleY: 0.72 }] }, grid: { position: 'absolute', left: 0, right: 0, borderTopWidth: 1, borderTopColor: '#2B2D31', borderStyle: 'dashed' }, plot: { position: 'absolute', left: 0, right: 14, height: 2, borderRadius: 1 }, point: { position: 'absolute', right: -5, top: -4, width: 10, height: 10, borderRadius: 5, borderWidth: 2, borderColor: '#33404B' }, leftDate: { position: 'absolute', bottom: 0, left: 0, color: colors.textMuted, fontSize: 10 }, rightDate: { position: 'absolute', bottom: 0, right: 0, color: colors.textMuted, fontSize: 10 } });
